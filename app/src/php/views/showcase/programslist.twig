<ul class='showcase-content programslist-container'>
    {% for program in programsList %}
    <li>
        <img src='app/img/program-backgrounds/{{ program.name }}-sub.jpg' alt='{{ program.background_Alt }}' />
        <a href='{{ program.details_page_link }}' class='program-details'>
            <h2>{{ program.french_title }}</h2>
            <p><i class="fa-solid fa-clock"></i>{{ program.duration }} jours</p>
            <p>{{ program.subscription_price }} €</p>
            <span>
                <i class="fa-solid fa-plus"></i>
            </span>
        </a>
    </li>
    {% endfor %}
</ul>