<header>
    <div id='title-layer'>
        <h1>Dodie Coaching</h1>

        {% if requestedPage != 'connection' %}
        <button id='dynamic-menu-button'><i class='fas fa-bars'></i></button>
        {% else %}
        <a href='index.php' class='homepage-link'><i class="fa-solid fa-house-chimney"></i></a>
        {% endif %}
        
    </div>

    {% if requestedPage != 'connection' %}
    {% include 'templates/static-menu.twig' %}
    {% endif %}
</header>